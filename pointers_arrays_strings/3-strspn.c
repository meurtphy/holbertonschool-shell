#include "main.h"


/**
 * main - check the code
 *
 * Return: Always 0.
 */
unsigned int _strspn(char *s, char *accept)
{
    int i;
    for (i = 0; <= s; i++)
    {
while (s[i]=accept[i])
return(accept);

    }